---
en:
  ntfy:
    new_transaction:
      title: New transaction
      income: Income
      expense: Expense
    balance:
      title: Account balance (after sync)
      change_line: "Δ %{prior_days}d: %{balance_change} (was %{prior_balance})"
    summary:
      no_accounts: No accounts included in this summary.
    transaction:
      uncategorized_display: "⚠️ To categorize"
    actions:
      open_quick_categorize: Open in app
      open_account: Open account
      open_dashboard: Open dashboard
    default_templates:
      transaction_title: New transaction
      transaction_body: |
        [%{rule_name}] %{sign_label} %{amount}
        %{entry_name}
        %{date}
        %{account_name}
        %{category_name}
        %{quick_categorize_url}
      balance_title: "[%{rule_name}] %{account_name}"
      balance_body: "%{balance}%{balance_change_line}"
      summary_title: "[%{rule_name}] Financial summary"
      summary_body: |
        %{family_name} · %{generated_at}
        Accounts: %{accounts_count} (assets: %{asset_accounts_count}, liabilities: %{liability_accounts_count})
        Assets: %{total_assets}
        Liabilities: %{total_liabilities}
        Net worth: %{net_worth}

        Accounts
        %{accounts_breakdown}
