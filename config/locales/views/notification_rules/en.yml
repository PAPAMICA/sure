---
en:
  notification_rules:
    back: Back to notification rules
    edit: Edit
    unnamed: Untitled rule
    inactive: inactive
    confirm_destroy: Delete this notification rule?
    create:
      success: Notification rule created.
    update:
      success: Notification rule updated.
    destroy:
      success: Notification rule deleted.
    ntfy_settings_updated: Notification settings saved.
    trigger_deliver:
      button_title: Send one notification now (first matching transaction or account)
      success: Notification sent.
      no_ntfy: Set an ntfy topic URL in notification settings first.
      no_match: No transaction or account matches this rule yet.
      no_entry: Matching transaction has no entry; cannot send.
      delivery_failed: ntfy did not confirm delivery (non-2xx HTTP or network error). Check your URL, authentication, and server logs.
      failed: Could not send notification.
    test:
      send_button: Send test notification
      push_title: Test notification
      push_body: If you received this, your ntfy topic URL is configured correctly.
      success: Test notification sent successfully.
      failure: Could not reach ntfy (invalid URL or network error). Check the logs.
      http_error: "ntfy returned HTTP %{code}. Check your URL and server logs."
      url_missing: Enter a topic URL in the form or save one first.
      inline_hint: "Test uses the URL and credentials in the form above (unsaved fields are included). Save to persist changes."
    errors:
      transaction_on_sync: Transaction rules cannot use “after account sync” delivery. Use immediate or scheduled.
      balance_immediate: Balance rules cannot use immediate delivery. Use after sync or scheduled.
    badges:
      target_transaction: Transactions
      target_balance: Balances
      delivery_immediate: Immediate
      delivery_scheduled: Scheduled
      delivery_on_sync: After account sync
    frequencies:
      hourly: Every hour
      every_4_hours: Every 4 hours
      daily: Daily
      weekly: Weekly
    filters:
      balance_account: Account
    index:
      title: Notification rules
      new_transaction_rule: New transaction rule
      new_balance_rule: New balance rule
      intro_before_link: Send alerts through
      intro_after_link: Create rules with IF conditions (like auto-categorization), optional minimum amount, and how often to check.
      ntfy_auth_doc_prefix: "Servers with access control need authentication — see "
      ntfy_auth_doc_link: ntfy authentication
      ntfy_settings_heading: Notification settings (ntfy)
      ntfy_settings_intro: "Connection, credentials, and message templates apply to all rules. Only family admins can edit this section."
      ntfy_connection_heading: Connection
      ntfy_auth_hint: "Bearer token (recommended) or HTTP Basic. Leave token and password empty to keep saved values."
      secret_placeholder: Leave blank to keep current
      ntfy_access_token_label: Access token (Bearer)
      ntfy_basic_username_label: Basic auth username
      ntfy_basic_username_hint: Optional. Leave empty with only a password to use ntfy’s “empty user + token” Basic style.
      ntfy_basic_password_label: Basic auth password
      ntfy_token_family_hint: Paste a new token to replace the stored one. Leave empty to keep the current token.
      ntfy_password_family_hint: Paste a new password to replace the stored one. Leave empty to keep the current password.
      clear_ntfy_credentials_label: Clear all saved ntfy credentials (token, username, password)
      default_url_label: Topic URL
      templates_heading: Message templates
      templates_intro: "Use %{variable} placeholders. Leave a field empty to use the built-in default. New lines are preserved in the message body."
      template_transaction_heading: Transaction notifications
      template_variables_transaction: |
        Available variables:
        %{sign_label} — Income or expense label
        %{amount} — Formatted absolute amount
        %{entry_name} — Transaction name
        %{date} — Entry date (long format)
        %{account_name} — Account name
        %{category_name} — Category (may be empty)
        %{merchant_name} — Merchant (may be empty)
      template_transaction_title_label: Title template
      template_transaction_body_label: Body template
      template_balance_heading: Balance notifications (after sync / scheduled)
      ntfy_balance_prior_days_label: Compare to balance how many days ago?
      ntfy_balance_prior_days_hint: "Used for balance change and prior-balance template variables. Set to 0 to hide comparison. Range: 0–365."
      template_variables_balance: |
        Available variables:
        %{account_name} — Account name
        %{balance} — Formatted current balance
        %{currency} — Currency code
        %{balance_change} — Formatted difference (current − balance on or before that day)
        %{balance_change_abs} — Absolute change, formatted
        %{prior_balance} — Formatted balance on or before the comparison date
        %{prior_days} — Comparison window in days (same as the setting above)
        %{prior_date} — Calendar date used as the comparison anchor (long format)
        %{prior_balance_date} — Date of the ledger row used for the prior balance (long format)
        %{balance_change_line} — Localized one-line summary (leading newline when present; empty if no data)
      template_balance_title_label: Title template
      template_balance_body_label: Body template
      template_leave_blank_hint: Leave empty to use the default template.
      save_ntfy_settings: Save settings
      list_heading: Rules
      empty: No notification rules yet.
    new:
      title_transaction: New transaction notification rule
      title_balance: New balance notification rule
    edit_page:
      title: Edit notification rule
    form:
      name_label: Name
      name_placeholder: e.g. Large expenses on checking
      delivery_heading: When to notify
      delivery_immediate_tx: Immediately when a matching transaction is created
      delivery_on_sync: After each account sync (balance snapshot)
      delivery_scheduled: On a schedule (scan matching items)
      frequency_prompt: Select frequency
      frequency_hint: The scheduler runs hourly; your rule runs when it is due based on this frequency.
      active_label: Rule active
      ntfy_config_moved_hint: "ntfy URL, credentials, and message templates are configured in the notification settings above (not per rule)."
      amount_heading: Amount filter
      minimum_amount_label: Minimum absolute amount
      minimum_amount_hint: Only applies to transaction rules. Leave blank to include all amounts.
      all_dates: All %{resource} dates
      starting_from: Only from date
      add_condition: Add condition
      add_condition_group: Add condition group
      submit: Save rule
      add_sub_condition: Add condition
